# Orbiton Dependencies v2 compatible Orbiton v0->v7.0 Tokyo-class
# Install with: pip install -r requirements.txt

# Core: speech recognition (Google Web Speech API)
SpeechRecognition>=3.10.0

# TTS: neural text-to-speech (Microsoft Edge voices)
edge-tts>=6.1.0

# UI: pretty terminal colors, tables, panels
rich>=13.0.0

# Testing: pytest suite + coverage
pytest>=8.0.0
pytest-cov>=5.0.0